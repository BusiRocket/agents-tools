{
  "name": "busirocket-agents-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Agent workflow engine + rules/skills orchestrator for high-quality code across IDEs",
  "scripts": {
    "build": "pnpm run clean:dist && pnpm run rules:compile && pnpm run skills:compile",
    "check:all": "pnpm run format && pnpm run lint:fix && pnpm run rules:check && pnpm run skills:validate && pnpm run skills:test",
    "check:ci": "pnpm run check:all",
    "check": "pnpm run check:all",
    "format:check": "prettier --check .",
    "format": "prettier --write . --list-different",
    "lint:fix": "eslint . --fix",
    "lint": "eslint .",
    "clean:dist": "node -e \"require('node:fs/promises').rm('dist', { recursive: true, force: true })\"",
    "rules:check": "tsx scripts/commands/compile-rules.ts --check",
    "rules:compile": "tsx scripts/commands/compile-rules.ts",
    "rules:verify": "tsx scripts/commands/verify-rules-output.ts",
    "rules:link": "tsx scripts/commands/link-rules-global.ts",
    "skills:compile": "tsx scripts/commands/compile-skills.ts",
    "skills:inventory": "tsx scripts/commands/skills-inventory.ts",
    "skills:link": "tsx scripts/commands/link-skills-global.ts",
    "skills:link:antigravity": "tsx scripts/commands/runInstallAntigravitySkills.ts",
    "skills:package": "tsx scripts/commands/package-skills.ts",
    "skills:llms": "tsx scripts/commands/generate-llms-txt.ts",
    "skills:prompt:file": "tsx scripts/commands/to-prompt.ts > available_skills.xml",
    "skills:prompt": "tsx scripts/commands/to-prompt.ts",
    "skills:test": "tsx scripts/commands/test-skills.ts",
    "skills:validate": "tsx scripts/commands/validate-skills.ts",
    "skills:version:check": "tsx scripts/commands/check-version.ts",
    "sync": "pnpm install && pnpm run build && pnpm run check && pnpm run rules:link && pnpm run skills:link",
    "update": "pnpm update && pnpm run sync",
    "validate:install": "python3 -m venv .venv-validate && .venv-validate/bin/pip install skills-ref"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.5.0",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-boundaries": "^6.0.1",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-sonarjs": "^4.0.2",
    "eslint-plugin-unicorn": "^63.0.0",
    "eslint-plugin-unused-imports": "^4.4.1",
    "globals": "^17.4.0",
    "prettier": "^3.8.1",
    "ts-morph": "^27.0.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.57.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/BusiRocket/busirocket-agents-tools.git"
  },
  "license": "MIT"
}
